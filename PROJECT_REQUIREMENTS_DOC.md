# 环影（Huanying）项目需求文档

**文档版本**: v13.0  
**更新日期**: 2026年7月29日  
**项目状态**: 正式发布

---

## 1. 项目概述与背景

### 1.1 项目简介

**环影**（原名mplay）是一款基于Electron的跨平台桌面媒体播放器，支持连接Jellyfin和Emby流媒体服务器，同时支持本地文件播放。项目以高性能播放、流畅用户体验和丰富的弹幕系统为核心卖点。

### 1.2 项目背景

随着流媒体服务器（Jellyfin、Emby）的普及，用户对跨平台、功能丰富的媒体播放器需求日益增长。环影旨在提供：
- 统一的媒体播放解决方案
- 丰富的弹幕互动体验
- 高性能、低延迟的播放引擎
- 美观、现代的用户界面

### 1.3 目标用户

- 家庭媒体服务器用户（Jellyfin/Emby用户）
- 本地高清视频爱好者
- ACG（动漫、漫画、游戏）内容消费群体
- 追求高画质、高音质的影音发烧友

### 1.4 项目定位

| 维度 | 说明 |
|------|------|
| 产品类型 | 桌面媒体播放器 |
| 目标平台 | Windows 10/11 (64位) |
| 核心价值 | 多源融合播放 + 弹幕互动 + 极致体验 |
| 竞争优势 | 原生弹幕引擎、MPV高性能播放、精美UI设计 |

---

## 2. 详细功能模块说明

### 2.1 媒体库管理模块

#### 2.1.1 服务器连接管理

**功能描述**：支持添加、管理多个Jellyfin/Emby服务器连接。

**主要功能点**：
- 添加Jellyfin服务器（URL + API Token）
- 添加Emby服务器（URL + 用户名 + 密码）
- 服务器连接测试与状态检测
- 多服务器切换
- 服务器信息加密存储（XOR混淆 + safeStorage）

**用户交互流程**：
1. 用户进入"设置"页面
2. 点击"服务器管理"区域的"添加服务器"
3. 选择服务器类型（Jellyfin/Emby）
4. 填写服务器URL和认证信息
5. 点击"测试连接"验证
6. 测试成功后保存服务器配置
7. 在服务器列表中选择激活的服务器

**业务逻辑**：
- 服务器配置使用XOR加密存储敏感信息
- 支持Emby登录流程（获取Token和UserId）
- 连接测试包含超时机制（5秒）

#### 2.1.2 媒体浏览

**功能描述**：提供直观的媒体内容浏览和搜索功能。

**主要功能点**：
- 按类型分类浏览（电影、剧集、音乐）
- 文件夹层级导航
- 关键词搜索
- 最近入库展示
- 虚拟滚动列表（支持万级内容流畅滚动）

**用户交互流程**：
1. 首页展示媒体库分类入口
2. 用户点击分类进入浏览页面
3. 支持返回上级目录
4. 支持搜索过滤
5. 点击内容卡片进入详情页

**业务逻辑**：
- 使用`@tanstack/react-virtual`实现虚拟滚动
- 海报图懒加载
- API请求缓存（2分钟TTL）
- 并发请求控制（6个并发限制）

#### 2.1.3 详情页展示

**功能描述**：展示媒体详细信息，包括演员、剧情简介、评分等。

**主要功能点**：
- 海报/背景图展示
- 基础信息（年份、评分、时长）
- 演员列表
- 剧集列表（电视剧）
- 收藏状态

**用户交互流程**：
1. 从媒体库点击内容卡片进入详情页
2. 查看详细信息
3. 点击"播放"按钮开始播放
4. 剧集可选择具体集数

**业务逻辑**：
- 自动匹配本地弹幕缓存
- 预下载弹幕数据
- 支持豆瓣海报补充

---

### 2.2 媒体播放模块

#### 2.2.1 MPV播放引擎

**功能描述**：基于MPV（libmpv）的高性能媒体播放引擎。

**主要功能点**：
- 全格式视频支持（H.264、H.265/HEVC、AV1等）
- 多音轨切换
- 字幕支持（内嵌字幕、外部字幕文件）
- 硬件解码（GPU加速）
- 全屏播放
- 画面截图
- 倍速播放（0.5x - 4x）

**用户交互流程**：
1. 点击播放按钮启动播放器
2. 加载媒体文件
3. 显示播放控制栏
4. 用户可调整播放参数
5. 支持快捷键操作

**业务逻辑**：
- 使用`node-mpv`库控制MPV进程
- 播放状态事件驱动（暂停/播放/跳转）
- 进度实时上报服务器
- 断点续播支持

#### 2.2.2 播放控制界面

**功能描述**：现代化的播放控制界面，支持多种交互方式。

**主要功能点**：
- 播放/暂停控制
- 进度条拖动
- 音量调节
- 倍速选择
- 轨道切换
- 快捷键支持
- 上下文菜单

**快捷键列表**：
| 按键 | 功能 |
|------|------|
| Space | 播放/暂停 |
| ← / → | 快退/快进5秒 |
| ↑ / ↓ | 音量调节 |
| F | 全屏切换 |
| M | 静音切换 |
| S | 截图保存 |

#### 2.2.3 历史记录管理

**功能描述**：自动记录用户的观看历史，支持快速恢复。

**主要功能点**：
- 自动保存播放进度
- 历史记录列表展示
- 一键续播
- 历史记录删除
- 清空历史

**用户交互流程**：
1. 播放开始后自动记录
2. 暂停/跳转时更新进度
3. 退出时保存最终进度
4. 在"历史"页面查看记录
5. 点击记录继续播放

**业务逻辑**：
- 支持服务器进度同步（Jellyfin progress API）
- 本地文件进度独立存储
- 历史记录保留最近100条

---

### 2.3 弹幕系统模块

#### 2.3.1 弹幕引擎

**功能描述**：高性能弹幕渲染引擎，支持多种弹幕类型。

**主要功能点**：
- 滚动弹幕（从右向左）
- 顶部固定弹幕
- 底部固定弹幕
- 颜色自定义
- 字号调整
- 透明度调节
- 防碰撞检测

**技术实现**：
- 基于Canvas渲染
- 二叉搜索算法优化碰撞检测
- 时间驱动的弹幕生命周期管理
- 统一videoTime时钟

**弹幕模式**：
| 模式 | 说明 | 对应B站规范 |
|------|------|-------------|
| rtl | 右向左滚动 | mode=1 |
| ltr | 左向右滚动 | mode=6 |
| top | 顶部固定 | mode=5 |
| bottom | 底部固定 | mode=4 |

#### 2.3.2 弹幕API集成

**功能描述**：对接第三方弹幕API，自动匹配和加载弹幕。

**主要功能点**：
- 弹幕API配置（主API + 镜像）
- 关键词搜索匹配
- 自动标题清洗匹配
- 弹幕预下载缓存
- 本地XML弹幕支持

**用户交互流程**：
1. 在设置中配置弹幕API地址
2. 播放视频时自动搜索匹配
3. 显示匹配结果供选择
4. 加载弹幕到播放器
5. 弹幕显示/隐藏切换

**业务逻辑**：
- 标题清洗算法（去除特殊字符、标点）
- 多来源支持（dandanplay等）
- 本地XML弹幕解析
- 弹幕缓存管理

#### 2.3.3 弹幕设置面板

**功能描述**：实时调整弹幕显示参数。

**主要功能点**：
- 字号调节（12-32px）
- 滚动速度（60-300px/s）
- 显示区域（全屏/顶部/底部）
- 透明度（0-100%）
- 弹幕开关

---

### 2.4 设置配置模块

#### 2.4.1 外观设置

**功能描述**：个性化应用外观。

**主要功能点**：
- 主题切换（暗色/亮色/跟随系统）
- 窗口始终置顶
- 界面动画开关

#### 2.4.2 播放设置

**功能描述**：配置播放器行为。

**主要功能点**：
- 硬件解码开关
- HDR色调映射
- 默认播放速度
- 字幕默认开启

#### 2.4.3 网络设置

**功能描述**：配置网络相关参数。

**主要功能点**：
- Jellyfin服务器配置
- Emby服务器配置
- 弹幕API配置
- 豆瓣API配置

#### 2.4.4 数据管理

**功能描述**：数据备份与恢复。

**主要功能点**：
- 配置导入/导出
- 播放历史清理
- 弹幕缓存清理
- 日志查看

---

### 2.5 系统交互模块

#### 2.5.1 窗口管理

**功能描述**：现代化的窗口操作体验。

**主要功能点**：
- iOS风格窗口控制按钮
- 无边框窗口拖动
- 窗口置顶
- 最小化到托盘

#### 2.5.2 托盘功能

**功能描述**：系统托盘集成。

**主要功能点**：
- 托盘图标显示
- 托盘菜单（打开/退出）
- 正在播放提示

#### 2.5.3 全局快捷键

**功能描述**：系统级快捷键支持。

**主要功能点**：
- 全局播放/暂停
- 全局下一首
- 全局音量控制

---

## 3. 技术架构与框架选型

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Electron App                          │
├─────────────────────────────────────────────────────────────┤
│  Main Process (主进程)  │  Renderer Process (渲染进程)       │
│  ┌─────────────────┐   │  ┌─────────────────────────────┐  │
│  │ - BrowserWindow │   │  │ - React 19 UI               │  │
│  │ - IPC Handlers  │◄─►│  │ - TailwindCSS 样式           │  │
│  │ - MPV Controller│   │  │ - Framer Motion 动画        │  │
│  │ - File System   │   │  │ - Zustand 状态管理          │  │
│  │ - Network       │   │  │ - HashRouter 路由           │  │
│  │ - Config Store  │   │  │ - Danmaku Engine 弹幕       │  │
│  └─────────────────┘   │  └─────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  Preload Script (预加载脚本)                                │
│  - contextBridge API 暴露                                  │
│  - ipcRenderer 事件桥接                                     │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 19.x | UI框架 |
| TypeScript | 5.6.x | 类型安全 |
| TailwindCSS | 4.x | 样式框架 |
| Framer Motion | 12.x | 动画库 |
| React Router | 7.x | 路由管理 |
| Zustand | 5.x | 状态管理 |
| lucide-react | 1.x | 图标库 |
| @tanstack/react-virtual | 3.x | 虚拟滚动 |

### 3.3 后端技术栈（Electron主进程）

| 技术 | 版本 | 用途 |
|------|------|------|
| Electron | 33.x | 桌面框架 |
| Node.js | (内置) | 运行时 |
| node-mpv | 2.0.0-beta | MPV控制 |
| @electron-toolkit | 3.x | Electron工具 |

### 3.4 数据存储

| 存储类型 | 实现方式 | 用途 |
|---------|---------|------|
| 配置存储 | Electron app.getPath('userData') | 用户配置 |
| 加密存储 | safeStorage + XOR混淆 | 敏感信息 |
| 日志文件 | 文件系统（JSON格式） | 运行日志 |
| 弹幕缓存 | 文件系统 | 弹幕数据缓存 |
| localStorage | 浏览器存储 | 主题偏好 |

### 3.5 第三方API依赖

| API | 用途 | 配置项 |
|-----|------|--------|
| Jellyfin API | 媒体服务器通信 | 服务器URL + Token |
| Emby API | Emby服务器通信 | 服务器URL + 账号密码 |
| 弹幕API | 弹幕数据获取 | API地址 |
| 豆瓣API | 评分/海报获取 | API地址 |

### 3.6 项目目录结构

```
logvar-player/
├── src/
│   ├── main/                    # 主进程
│   │   ├── index.ts            # 主进程入口
│   │   ├── mpv-controller.ts   # MPV控制器
│   │   ├── log-window.html     # 日志窗口
│   │   └── services/
│   │       └── poster-cache.ts  # 海报缓存服务
│   ├── preload/                 # 预加载脚本
│   │   ├── index.ts            # API桥接
│   │   └── log-preload.ts      # 日志预加载
│   ├── renderer/                # 渲染进程
│   │   ├── src/
│   │   │   ├── components/     # UI组件
│   │   │   ├── pages/          # 页面组件
│   │   │   ├── features/       # 功能模块
│   │   │   ├── providers/      # 状态提供者
│   │   │   ├── utils/          # 工具函数
│   │   │   └── styles/         # 样式
│   │   └── index.html          # HTML入口
│   └── shared/                  # 共享类型
│       ├── types.ts            # 通用类型
│       ├── preload-types.ts    # API类型
│       └── typed-values.ts     # 常量定义
├── build/                       # 构建资源
│   ├── icon.ico                # 应用图标
│   ├── icon-*.png              # 多尺寸图标
│   └── ...
├── scripts/                     # 构建脚本
│   ├── installer.nsh           # NSIS安装脚本
│   ├── build-win.mjs           # Windows构建
│   └── ...
├── electron-builder.yml        # 打包配置
├── electron.vite.config.cts    # Vite配置
├── package.json                # 包配置
└── tsconfig.json               # TypeScript配置
```

---

## 4. 系统环境与部署要求

### 4.1 运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 version 1809 (2018年10月更新) 及以上 |
| 架构 | x64 (64位) |
| 内存 | 建议8GB RAM以上 |
| 磁盘空间 | 至少500MB可用空间 |
| 网络 | 首次使用需要网络连接（媒体服务器通信） |

### 4.2 开发环境

| 工具 | 版本要求 |
|------|---------|
| Node.js | 20.x 或更高版本 |
| npm | 10.x 或更高版本 |
| Visual Studio Code | 最新版本（推荐） |
| Git | 2.x 或更高版本 |

### 4.3 构建与打包

**开发模式**：
```bash
npm install
npm run dev
```

**生产构建**：
```bash
npm install
npm run build
npm run build:win
```

**打包产物**：
- 安装包: `dist/环影-13.0.0-setup.exe`
- 解压版: `dist/win-unpacked/`

### 4.4 部署要求

1. 目标系统需安装VC++运行时（通常随Windows 10/11自动安装）
2. 需要管理员权限安装（可选，取决于安装路径）
3. 媒体服务器需预先部署（Jellyfin/Emby）
4. 网络环境需允许与媒体服务器通信

---

## 5. 性能指标与安全规范

### 5.1 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 启动时间 | < 3秒 | 冷启动到可交互 |
| 页面切换 | < 300ms | 路由切换响应 |
| 视频加载 | < 1秒 | MPV启动到画面显示 |
| 弹幕渲染 | 60fps | 流畅动画 |
| 内存占用 | < 500MB | 正常使用场景 |
| CPU占用 | < 15% | 视频播放时 |

### 5.2 网络性能

| 指标 | 目标值 |
|------|--------|
| API响应缓存 | 2分钟TTL |
| 图片懒加载 | 视口外不加载 |
| 并发请求限制 | 6个同时 |
| 进度上报间隔 | 30秒 |

### 5.3 安全规范

#### 5.3.1 数据安全

- **敏感信息加密**：服务器密码、Token使用XOR混淆 + safeStorage双重保护
- **配置文件隔离**：用户数据存储在用户专属目录，不可被其他用户访问
- **日志脱敏**：敏感信息不出现在日志中

#### 5.3.2 进程安全

- **Context Isolation**：启用上下文隔离，preload脚本无法访问渲染页面
- **Node Integration**：渲染进程禁用Node.js集成
- **IPC白名单**：仅允许预定义的IPC通道通信

#### 5.3.3 网络安全

- **HTTPS优先**：所有API请求优先使用HTTPS
- **Token认证**：媒体服务器API使用Token认证
- **输入验证**：所有用户输入经过验证和清洗

### 5.4 错误处理

| 场景 | 处理方式 |
|------|---------|
| 网络断开 | 优雅降级，显示离线提示 |
| API超时 | 30秒超时，用户可重试 |
| 播放失败 | 显示错误信息，支持切换播放器 |
| 渲染崩溃 | ErrorBoundary捕获，支持刷新 |
| 磁盘空间不足 | 警告提示，阻止关键操作 |

---

## 6. 未来功能扩展规划

### 6.1 短期规划（v14.0）

#### 6.1.1 功能增强

- [ ] **多语言支持**：支持中文、英文、日文
- [ ] **主题商店**：更多主题皮肤选择
- [ ] **播放列表**：自定义播放列表管理
- [ ] **智能推荐**：基于观看历史的内容推荐

#### 6.1.2 体验优化

- [ ] **启动加速**：预加载关键资源
- [ ] **手势支持**：触控板手势操作
- [ ] **键盘映射**：自定义快捷键
- [ ] **多窗口**：支持同时打开多个视频

### 6.2 中期规划（v15.0）

#### 6.2.1 新功能

- [ ] **跨平台支持**：macOS、Linux版本
- [ ] **云同步**：配置云端同步
- [ ] **多用户**：支持多用户配置
- [ ] **投屏功能**：Chromecast/AirPlay支持

#### 6.2.2 生态集成

- [ ] **插件系统**：支持第三方插件
- [ ] **API开放**：提供开发者API
- [ ] **社区模板**：共享配置方案

### 6.3 长期规划（v20.0）

#### 6.3.1 技术演进

- [ ] **AI集成**：智能内容分析
- [ ] **VR/AR支持**：沉浸式观影体验
- [ ] **PWA支持**：Web版本
- [ ] **微服务架构**：模块化重构

#### 6.3.2 平台扩展

- [ ] **移动端**：iOS/Android App
- [ ] **电视端**：Smart TV应用
- [ ] **车载系统**：CarPlay/Android Auto

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| MPV | 开源媒体播放器内核，基于libmpv |
| IPC | 进程间通信（Inter-Process Communication） |
| TTL | 生存时间（Time To Live） |
| XOR | 异或运算，用于数据加密 |
| Context Isolation | Electron安全特性，隔离上下文 |
| NSIS | Nullsoft Scriptable Install System |

### B. 参考文档

| 文档 | 路径 |
|------|------|
| 技术设计文档 | `TECHNICAL_DESIGN.md` |
| 发布操作文档 | `RELEASE_OPERATION_DOC.md` |
| 图标设计研究 | `ICON_DESIGN_RESEARCH.md` |
| 更新日志 | `CHANGELOG.md` |

### C. 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v13.0 | 2026-07-29 | 应用名称改为"环影"，移除DandanPlay认证，优化安装器 |
| v1.2 | 2026-07-23 | 修复安装器卡死问题，优化弹幕测试反馈 |
| v1.0 | 2026-07-01 | 初始版本发布 |

---

**文档维护者**: 环影开发团队  
**最后更新**: 2026年7月29日
